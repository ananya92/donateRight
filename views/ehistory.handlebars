<div id="event-list" class="box">
  <article class="panel is-primary">
      <p class="panel-heading .has-text-centered">
          Event History:
      </p>
      <br>
      <aside class="menu">
          <ul class="menu-list">
              {{#each dbEvents}}
              <div class="box">
                <li data-id={{dataValues.id}}>
                    <div class="content is-small">
                        <h2>Description: {{dataValues.description}}</h2>
                    </div>
                </li>
              </div>
              {{/each}}
          </ul>
      </aside>
  </article>
</div>